function open(url){
    window.open(url);
};
function getBarValue(bar){
    search = bar.value
    console.log(search);
};
let searchBar = document.getElementById("search");
let search = searchBar.value
